// This file is kept for compatibility but no longer used in the frontend-only version
// It can be safely removed if no other files depend on it

export const dummyExport = true;
// The original queryClient configuration has been removed as it's no longer needed
